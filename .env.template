# Required for lib/geo-services.ts
PUBLIC_BASE_URL=https://your-service-url.a.run.app
TASK_SECRET=some-long-random-secret
SCAN_TASK_QUEUE=geo-scan-queue
COMPUTE_TASK_QUEUE=geo-compute-queue
TASKS_LOCATION=us-central1

# Required for lib/gcp/firestore.ts (if running locally with service account)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/credentials.json

# Optional
GCP_PROJECT=your-project-id
